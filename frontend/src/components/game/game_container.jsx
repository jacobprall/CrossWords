import GameIndex from './game_index'
import {connect} from 'react-redux';
// import {createGame, updateGame, fetchWord} from '../../actions/game_actions';

const mSTP = state => ({

})

const mDTP = dispatch => ({
    
})

export default connect(mSTP, mDTP)(GameIndex);