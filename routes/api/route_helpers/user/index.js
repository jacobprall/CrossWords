const loginCallback = require('./login');
const registerCallback = require('./register');
const currentUserCallback = require('./currentUser');
const currentUserStats = require('./stats');

module.exports = {
  loginCallback,
  registerCallback,
  currentUserCallback,
  currentUserStats,
};
